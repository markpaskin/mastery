package us.paskin.mastery;

/**
 * Represents a concrete practice session.
 */
public class Session {
    /**
     * Generates a new session for the provided schedule.
     *
     * @return
     */
    public static Session sampleSession(Proto.Schedule schedule,
                                        Model model) {
        return null;
    }
}
